language: generic 
sudo: required
services:
  - docker

before_install: 
  - docker build -t shubhra27/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shubhra27/docker-react npm run test